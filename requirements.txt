# SlideArabi 1.0.0 — Python dependencies for Railway deployment
# ───────────────────────────────────────────────────────────

# Web server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# PowerPoint processing
python-pptx==1.0.2

# XML processing (used by rtl_transforms, typography, utils, vqa_engine, etc.)
lxml>=5.0

# Payment processing
stripe==11.4.1

# Image processing (for VQA slide rendering)
Pillow>=10.0

# File upload support (required by FastAPI for UploadFile/Form)
python-multipart>=0.0.9

# Pydantic (FastAPI dependency, pin for stability)
pydantic>=2.0,<3.0

# LibreOffice is handled at OS level in Dockerfile (for slide preview rendering)
